import React from "react";
import PTDconPNG from './ptd.png'; // Đường dẫn đến tệp PNG

const PTDIcon: React.FC<any> = () => (
    <img src={PTDconPNG} alt="PTD Icon" width={44} height={45} />
);

export default PTDIcon;
