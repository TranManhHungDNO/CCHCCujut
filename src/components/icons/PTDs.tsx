import React from "react";
import PTDsconPNG from './ptds.png'; // Đường dẫn đến tệp PNG

const PTDsIcon: React.FC<any> = () => (
    <img src={PTDsconPNG} alt="PTDs Icon" width={44} height={45} />
);

export default PTDsIcon;
